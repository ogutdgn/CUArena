# Implementation Plan: Table Styles Catalog + Visual Gallery + Theme Palette

**Branch**: `parity-v2` (fix loop lands directly per standing directive) | **Date**: 2026-07-02 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `/specs/030-table-styles-gallery/spec.md`

## Summary

Close the 2/247 gallery gap (Phase B named gap §1) + the theme-palette divergence (VISUAL L4
finding): register the 113 real-Word modern table-style definitions behind a lazy bridge
materializer wrapped around the fork's `setTableStyle`; fix the stale legacy literal hexes in the
fork's fallback GT4A1 data (data-only); replace the 2-item text flyout with Word's in-ribbon tile
gallery (carousel strip + sectioned More flyout + Modify/Clear/New footer) with
`addToHistory:false` hover live-preview mirroring `style-preview.ts`.

## Technical Context

**Language/Version**: TypeScript (renderer bridges, electron-vite) + vanilla JS chrome (`window.WC`)
**Primary Dependencies**: the vendored SuperDoc fork (read-only), existing ribbon gallery infra
(`makeGalleryCarousel`), `parity/oracle/table_style_defs.json` + `word_theme_palette.json` (ground truth)
**Storage**: N/A (document model + generated static module)
**Testing**: 3 standing gates (test:pm/smoke/roundtrip) + the certified parity pipeline
(`run.py --only tb-style-*`, behavior cards, scorecard, VISUAL pairs)
**Target Platform**: Electron desktop (Windows primary)
**Project Type**: desktop app
**Performance Goals**: gallery opens instantly with 113 tiles (pure-DOM thumbs, no canvas);
preview enter/leave < 100ms perceived
**Constraints**: NO fork logic edits; zero undo pollution from preview; blank-doc styles.xml stays
lean (lazy materialization)
**Scale/Scope**: 113 style defs (~400KB raw XML in the generated module — acceptable; entry-size
gate `test:bundle` must stay green, code-split if needed)

## Constitution Check

- **I. No Fork Edits** — ⚠️ ONE explicit exception requested (this section is the decision record):
  `exporter-docx-defs.js DEFAULT_LINKED_STYLES['GridTable4-Accent1']` carries STALE legacy literal
  hexes (4472C4/8EAADB) cached beside themeColor refs; the renderer prefers literals
  (application.ts:228) so the fallback def renders the wrong palette on every blank doc. Fix =
  **data-only value replacement** with the locked-build capture (same verbatim def as the oracle
  catalog). No logic changes. Marked with the standard fork-edit comment convention. Everything
  else in this feature is bridge/chrome-side.
- **II. Single Document-Write Path** — all writes via WC.PM bridge verbs (`tableSetStyle`,
  the new materializer, preview enter/leave). ✅
- **III. Page-Free Document Model** — untouched. ✅
- **IV. Real-Word Fidelity, Oracle-Validated** — defs are verbatim locked-build captures;
  acceptance = the certified pipeline (tb-style-listtable3, VISUAL re-judge). ✅
- **V. Test-Gated** — per-piece test:pm additions + 3 gates + parity re-measure. ✅
- **VI. Spec-Kit-Driven** — this document. ✅
- **VII. Generated Files Never Hand-Edited** — `table-style-defs.ts` generated by
  `scripts/gen-table-style-defs.js` with the AUTO-GENERATED banner. ✅

## Project Structure

### Documentation (this feature)

```text
specs/030-table-styles-gallery/
├── spec.md
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
└── tasks.md
```

### Source (repository)

```text
scripts/gen-table-style-defs.js            [NEW generator]
src/renderer/core/generated/table-style-defs.ts   [GENERATED catalog module]
src/renderer/bridge/table-styles.ts        [NEW: ensureTableStyleMaterialized + preview enter/leave]
src/renderer/bridge/table.ts               [EDIT: tableSetStyle wraps materializer; getTableStyles unions catalog]
src/renderer/bridge/index.ts               [EDIT: install new bridge piece]
src/renderer/core/superdoc-fork/core/super-converter/exporter-docx-defs.js
                                           [DATA-ONLY fork edit: GT4A1 stale literals → locked-build values]
src/renderer/public/js/ribbon.js           [EDIT: renderTableStylesGroup + tableStyleThumb (archive-adapted)]
src/renderer/public/js/table-tools-pm.js   [EDIT: td-styles group renders the gallery]
src/renderer/public/js/commands.js         [EDIT: H.tblStyles footer (Modify/Clear/New)]
src/renderer/public/css/ribbon.css         [EDIT: .tblstyle-* classes (archive-adapted)]
scripts/test-suite-pm.js                   [EDIT: regression tests]
```

## Phase log

- Phase 0 research: [research.md](research.md) — all unknowns resolved (seam, theme root cause,
  preview mechanism). No NEEDS CLARIFICATION remain.
- Phase 1 design: [data-model.md](data-model.md), [quickstart.md](quickstart.md). Contracts dir
  skipped — no external interface; the "contract" is the parity pipeline's acceptance gates
  (quickstart lists them as runnable commands).

## Implementation order (feeds tasks.md)

1. **T-theme**: data-only GT4A1 fallback fix (fork data) + pm test (blank doc → apply GT4A1 →
   exported def carries 156082-derived literals).
2. **T-catalog**: generator + generated module + `ensureTableStyleMaterialized` +
   `tableSetStyle` wrap + `getTableStyles` union + pm tests (apply ListTable3 → styles.xml has
   def; import-precedence: open docx with own def → not clobbered; double-apply → one def).
3. **T-gallery**: ribbon gallery (carousel strip, tiles, sections in More, footer, active tile)
   + pm/probe assertions (gallery count ≥ 113, footer items present, Clear works).
4. **T-preview**: `tableStylePreviewEnter/Leave` (addToHistory:false, setState restore, hop
   contract, input-cancel, tab-teardown restore) + pm tests (history length unchanged after
   enter/leave; doc identical after leave).
5. **T-measure**: gates + `run.py --only tb-style-listtable3 --capture-clone --import-leg` +
   `--only tb-style-grid4a1` + behavior re-run + scorecard re-run + VISUAL re-capture/re-judge
   (table-styles-gallery, doc-styled-table) + ledger refresh + adversarial review.

Subagent split (hybrid mode): T-catalog generator + T-theme data swap = mechanical (Opus);
T-gallery = medium (Opus, archive code supplied); T-preview = care (Opus draft + my review of the
setState/hop/teardown edges); T-measure = orchestrator (me).
