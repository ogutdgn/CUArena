import { Section } from "./sectionShell";

export function ExportSection() {
  return (
    <Section title="Export" addId="export.add">
      <div style={{ color: "var(--color-text-muted)", fontSize: "var(--fs-xs)" }}>
        Visual-only in this mock.
      </div>
    </Section>
  );
}
