# Progress — 2026-05-25 — W0 done, W1 kicked off

Branch: `improve-lo-test`.

## Done this session

### W0 — Foundations (complete, committed `dc2b6e041`)
- All decisions locked (DECISIONS.md D1–D8 + icons): Boundary A, Qt6+QML,
  Fluent, Writer-only, engine-as-dependency, logger-in-our-layer, Docker-binary.
- LOK feasibility research → `architecture/LOK_REFERENCE.md` (verdict: feasible).
- Wrote ARCHITECTURE.md, LOGGING.md, DECISIONS.md, last-point, execution-map,
  CLAUDE.md/AGENTS.md/README; repo-root app table updated.
- Permissions allowlist in `.claude/settings.local.json` (gitignored).

### W1 — Engine (in progress)
- **Engine state probed:** ext4 (build OK here), **not configured, not built,
  no workdir, `external/tarballs` cache empty** → cold build ≈ 3 h.
- **Toolchain probe:** gcc13/g++/make/autoconf/automake/bison/flex/gperf/nasm/
  zip/unzip/pkg-config/python3 present. **Missing:** `libtool`, `ccache`,
  `libcups`-dev. **Qt6 entirely missing** (needed for our app, W2).
- **Build recipe finalized** → `architecture/ENGINE_BUILD.md`. All flags
  verified against `configure.ac`. Key choice: `--disable-gtk3/gtk4/qt*/kf*`
  + `--disable-cups/dbus/avmedia/gstreamer` + `--enable-mergelibs`
  `--enable-release-build` — engine runs **headless (svp)**; our Qt6 UI is
  separate; we use the **core LOK C API**, not the gtk glue.
- **Command catalog built** (committed `4aff92bd8`): `tools/gen_command_catalog.py`
  → `resources/command-catalog.json`, **1520 commands** (977 generic + 543
  writer) with labels/tooltips/semanticName/propertiesRaw. Build-independent,
  verified. Feeds UI + dispatch + logger + MCP.
  - Note: `Properties` bit meanings NOT asserted (stored raw). Observed
    toggleable formatting cmds = 9, others = 1 → the earlier research guess
    "8=hidden" is wrong; verify against officecfg `.xcs` in W3.
- **Writer functionality map extracted** (build-independent): `tools/extract_menu_tree.py`
  parses the engine's authoritative `sw/uiconfig/swriter/menubar/menubar.xml`
  → `resources/writer-menu-tree.json`. 11 top-level menus (File / Edit / View
  / Insert / Format / Styles / Table / Form / Tools / Window / Help), 61
  menus, 497 items. **496/497 covered by our catalog** (the 1 miss is
  `vnd.sun.star.findbar:FocusToFindbar`, a protocol URL, not a .uno command).
  This is the deep-dive source for W3 (ribbon grouping) + W4 (dialog audit)
  and the measurable "every feature present" check the owner asked for.

## BLOCKER (owner action — sudo)

Build needs deps I can't install (no root). Owner runs in-session:

```
! sudo apt update && sudo apt install -y build-essential libtool ccache libnss3-dev && sudo apt build-dep -y libreoffice
```
(if `build-dep` says "no source package": Ubuntu 24.04 deb-src disabled —
enable sources or use an explicit list; ping me.)

Qt6 for our app (W2):
```
! sudo apt install -y cmake ninja-build qt6-base-dev qt6-declarative-dev qt6-base-dev-tools libqt6svg6-dev qml6-module-qtquick-controls qml6-module-qtquick-layouts
```

## UPDATE — deps installed, engine built, W1 DONE

Owner installed deps (`ccache`, Qt6 6.4.2 + cmake/ninja). `apt build-dep
libreoffice` failed (deb-src off) but **wasn't needed** — `configure` passed
all dependency checks with the present libs + LO's bundled externals.

- **configure** succeeded after restoring import-dropped gitignored files:
  `config.guess` + `missing` (from automake), `.vscode/...workspace.in`,
  `desktop/scripts/soffice.sh` (from LO upstream). Flags `--disable-gstreamer/
  cups/dbus`, `--enable-mergelibs/release-build` dropped (invalid/unconfirmed).
- **make** built **LO 26.8.0.0.alpha0** (instdir ~622 MB, 188 .so). The first
  run did ~all the compile and only failed at the final `soffice` packaging
  (missing soffice.sh); the resume finished in minutes.
- **Engine smoke**: `--version`, txt→docx, txt→pdf all OK.
- **LOK proof-of-life** (`tests/lok_proof_of_life.cpp`) **PASSED**: init →
  load Writer → paintTile (rendered) → `.uno:InsertText`+`.uno:Bold` → saveAs
  docx+odt; docx verified to contain our text + `<w:b/>`. **Boundary A proven.**
  Full gotchas list in ENGINE_BUILD.md §6.

## Next — W2 (Qt app skeleton + LOK binding)
See [`execution-map.md`](../execution-map.md) "Next: W2". Qt6 ready.
